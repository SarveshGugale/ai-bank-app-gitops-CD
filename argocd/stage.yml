apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ai-bankapp-stage
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/SarveshGugale/ai-bank-app-gitops-CD.git
    targetRevision: HEAD
    path: manifests/stage

  destination:
    server: https://kubernetes.default.svc
    namespace: bankapp-stage

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
